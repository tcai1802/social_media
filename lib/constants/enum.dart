enum BottomNavbarEnum {
  home,
  search,
  add,
  reels,
  profile,
}

enum AddTypeEnum {
  post,
  story,
  reel,
  live,
}

enum MediaType {
  image,
  video,
  reels,
  non,
}

enum UploadTypeEnum {
  camera,
  library,
}

enum SearchType {
  none,
  user,
}
